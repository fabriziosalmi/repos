name: Build and Deploy Dashboard

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  contents: write # Needed to push to the repository
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Cache Pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run validation tests
        run: python -m pytest

      - name: Generate stats data
        run: python stats.py
        env:
          MY_PAT: ${{ secrets.MY_PAT }}
      
      - name: Aggregate contributor statistics
        run: python aggregate_contributors.py docs/repositories-data.json docs/contributor-stats.json
      
      - name: Download previous data for comparison
        continue-on-error: true
        run: |
          # Download previous repositories-data.json from main branch
          git fetch origin main
          git show origin/main:docs/repositories-data.json > previous-data.json 2>/dev/null || echo "[]" > previous-data.json
      
      - name: Run Threshold Monitor
        continue-on-error: true
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          python threshold_monitor.py \
            --current docs/repositories-data.json \
            --previous previous-data.json

      - name: Generate badges and statistics
        run: |
          python generate_badges.py
          python update_readme_badges.py

      - name: Upload data artifact
        uses: actions/upload-artifact@v4
        with:
          name: stats-data
          path: |
            public_data/
            docs/badges/
            docs/STATS.md
            docs/stats-summary.json
            docs/contributor-stats.json
          retention-days: 1

  build-and-deploy:
    needs: build-data
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download data artifact
        uses: actions/download-artifact@v4
        with:
          name: stats-data
          path: frontend/public/ 

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend dependencies
        run: npm --prefix frontend ci

      - name: Build Vite application
        run: npm --prefix frontend run build

      - name: Prepare for deployment to docs/
        run: |
          rm -rf docs
          mkdir docs
          cp -r frontend/dist/* docs/
          touch docs/.nojekyll

      - name: Commit and push to docs/
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs
          git diff --staged --quiet || git commit -m "Deploy: Update GitHub Pages"
          git push