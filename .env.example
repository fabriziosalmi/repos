# GitHub Repository Analytics - Environment Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token
# Required for fetching repository data
# Create one at: https://github.com/settings/tokens
# Permissions needed: public_repo (or repo for private repos)
MY_PAT=your_github_personal_access_token_here

# Optional: Specify GitHub username (defaults to token owner)
# GITHUB_USERNAME=your_username

# Optional: Cache duration in hours (default: 1)
# CACHE_DURATION_HOURS=1

# Optional: Server port for local development (default: 8000)
# PORT=8000

# Optional: Enable verbose logging (default: false)
# VERBOSE=true

# Optional: Disable cache (default: false)
# NO_CACHE=false
